# Geometry Helpers

Implemented in `geometry.cpp`, with public API in
`../include/mars/perception_geometry/geometry.hpp`.
`test_geometry.cpp` covers primitives, angles, intersections, and invalid inputs.
Shared private helpers are in `../internal/`.
See the [module README](../README.md) for numerical conventions and build commands.
