# Graph and Bundle Tests

`test_graph_bundle_management.cpp` is a dependency-free offline test executable
registered with CTest. It covers the paper ranking formula, memory merging and
lifecycle filtering, BFS versus Dijkstra semantics, bundle ordering, reverse
sequence extraction, manager orchestration, and the unconfirmed gate-contract
failure path.
